/**
 * Re-export proxy — source physically lives in ./shared/data-table.tsx
 */
export {
  DataTable,
  autoSuggestColumnPinning,
  type ColumnConfig,
  type DataTableProps,
  type FreezeDirection,
  type SortDirection,
  type SortConfig,
  type GroupConfig,
} from "./shared/data-table"

export type { PaginationInfo } from "./shared/pagination"
